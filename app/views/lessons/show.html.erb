<h1><%= @lesson.title %></h1>

<p>Description: <%= @lesson.description %></p>
<p>Location: <%= @lesson.location %></p>
<p>When: <%= @lesson.when %></p>
<ul>
  <%= render partial: 'tags/tag', collection: @lesson.tags %>
</ul>

<%= link_to 'Edit Lesson', edit_teacher_lesson_path(@lesson.teacher, @lesson) if is_lesson_owner? %>

<%= link_to 'Enroll', lesson_enrollment_path(@lesson), method: :post unless is_enrolled? || is_teacher? %>

<%= link_to 'Drop Lesson', lesson_drop_path(@lesson), method: :delete if is_enrolled? %>